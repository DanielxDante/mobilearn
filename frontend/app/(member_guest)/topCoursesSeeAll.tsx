import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const topCoursesSeeAll = () => {
    return (
        <SafeAreaView>
            <Text>topCoursesSeeAll</Text>
        </SafeAreaView>
    );
};

export default topCoursesSeeAll;
